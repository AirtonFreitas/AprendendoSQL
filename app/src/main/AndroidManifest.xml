<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.airtonsiq.aprendendosql">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".activity_donate"
            android:exported="false" />
        <activity android:name=".activity_orderby" />
        <activity android:name=".congratulationsActivity" />
        <activity android:name=".tipodeDadosSQL" />
        <activity android:name=".dropActivity" />
        <activity android:name=".alterActivity" />
        <activity android:name=".deleteActivity" />
        <activity android:name=".selectActivity" />
        <activity android:name=".insertActivity" />
        <activity android:name=".updateActivity" />
        <activity android:name=".createActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>